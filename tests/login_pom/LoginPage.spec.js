// @ts-check
"use strict";
import { test, expect } from "@playwright/test";
import { defineConfig } from "@playwright/test";


// test("has no redirects", async ({ page }) => {
//   await page.goto("/app/business-signup");
//   await expect(page).toHaveURL(
//     "https://app-moccona.letsweel.com/app/business-signup"
//   );
//   await page.close();
});

// TODO: Get each warning message from each field
// i.e. each test should only test one fields warning message
// positive and negative tests for each field.

// TODO: Create account should only be clickable once all fields are correct.
// Check this ^^^

// TODO: the tests should be brokendown by page
// PageATest
// PageBTest
// etc..
